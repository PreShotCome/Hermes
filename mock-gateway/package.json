{
  "name": "pearl-mock-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal mock Pearl gateway for zero-config local mining (issues real matmul jobs)",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
